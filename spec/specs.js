describe('replacer', function(){
  it('is able to find a word in a string and replace it', function() {
    expect(replacer('hello world', 'hello', 'bye')).to.eql('bye world')
  });

  it('is able to find multiple same words and replace all of them')

});
